apiVersion: v1
kind: Service
metadata:
  name: keycloak-service
  namespace: tinder-space
spec:
  type: ClusterIP
  selector:
    app: keycloak
  ports:
    - name: http
      protocol: TCP
      port: 8088
      targetPort: 8080
