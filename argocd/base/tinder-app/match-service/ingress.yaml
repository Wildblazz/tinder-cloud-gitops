apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: match-ingress
  namespace: tinder-space
spec:
  ingressClassName: kong
  rules:
    - host: $(DOMAIN_NAME)
      http:
        paths:
          - path: /api/matches
            pathType: Prefix
            backend:
              service:
                name: match-service
                port:
                  number: 8084
